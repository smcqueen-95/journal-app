import React, { useState } from "react";
import { useAuth0 } from "@auth0/auth0-react";
import logo from "../assets/logo.png";

const CreateEntry = () => (
  <div className="text-left">
    <h1 className="mb-4">Create New Journal Entry</h1>
  </div>
);

export default CreateEntry;
